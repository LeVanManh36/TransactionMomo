syntax = 'proto3';

message Empty {}

message Request {
    string options = 1;
    string methodName = 2;
}
message Response {
    int32 code = 1;
    string message = 2;
    string data = 3;
}

service CampaignService {
    rpc Fetch (Request) returns (Response) {}
}
